import React from "react";

import StudentSign from "@/components/account/refer";

export const metadata = {
  title: "Student SignUp || PRIMS",
  description: "PRIMS platform",
};

const page = () => {
  return (
    <div>
      <StudentSign />
    </div>
  );
};

export default page;
